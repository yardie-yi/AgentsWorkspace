---
id: R004
status: enabled
name: build-before-deploy
applies-to: board-deploy
---

# R004 · 推板前必须完成编译

## 规则内容

执行推板操作前，必须确认 `build/` 目录下的产物是本次代码改动后重新编译的，不能用旧产物推板。

## 检查时机

推板前询问用户："本次产物是否已重新编译？"，用户确认后方可继续。

## 违规处理

用户未确认或明确未编译时，先引导完成编译再推板。
